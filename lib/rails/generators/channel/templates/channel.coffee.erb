# App.<%= channel_name %> = App.cable.subscriptions.create "<%= class_name %>",
  # collection: -> $("[data-channel='<%= channel_name %>']")

  # connected: ->
    # Called once the subscription has been successfully completed

  # received: (data) ->
    # Called when data is received
